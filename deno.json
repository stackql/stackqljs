{
  "lint": {
    "include": ["src/", "npm-support/"],
    "exclude": ["src/testing/", "src/**/*.test.ts", "src/*test.ts/", "*.js"],
    "rules": {
      "tags": ["recommended"],
      "include": [],
      "exclude": ["no-unused-vars", "ban-untagged-todo"]
    }
  },
  "fmt": {
    "useTabs": true,
    "lineWidth": 80,
    "indentWidth": 4,
    "semiColons": true,
    "singleQuote": true,
    "proseWrap": "preserve",
    "include": ["src/", "npm-support/", "node/"],
    "exclude": ["*.md", "*.json", "*.ts", "*.tsx", "*.jsx", "*.html"]
  },
  "tasks": {
    "test": "deno test --allow-net --allow-read --allow-write --allow-env --allow-run"
  }
}
